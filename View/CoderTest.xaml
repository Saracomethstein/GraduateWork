<Window x:Class="LearnCSharp.View.CoderTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LearnCSharp.View"
        mc:Ignorable="d"
        Title="CoderTest" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding CurrentTask.Description}" Grid.Row="0" Margin="10" />
        <TextBox Text="{Binding CurrentTask.UserCode, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="1" Margin="10" AcceptsReturn="True" Height="150" VerticalScrollBarVisibility="Auto"/>
        <Button Command="{Binding TestCodeCommand}" Grid.Row="2" Content="Test Code" Margin="10" />
        <TextBlock Text="{Binding CurrentTask.Output}" Grid.Row="3" Margin="10" />
    </Grid>
</Window>
